import {
  Clock,
  Comment,
  Deadline,
  Flow,
  Healthcare
} from '@qonsoll/react-native-design/src/assets'

const PARAMS = {
  speed: { icon: Comment },
  time: { icon: Clock },
  reps: { icon: Deadline },
  weight: { icon: Healthcare },
  distance: { icon: Flow }
}

export default PARAMS
